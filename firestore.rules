rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // analysisQuota is written only by analyzeCsv through the Admin SDK,
    // which bypasses these rules. Anonymous clients cannot read or reset it.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
